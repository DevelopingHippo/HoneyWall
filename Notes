Infrastructure:

Fix docker-compose.yml file:
- change default ports for MySQL
- Decide on which route to take (1 Database for both applications, or 2 separate databases)


MySQL Database:
- Create database layout for Website
- Create database layout for Application
- Create username and passwords for both and figure out a proper way to store them through Docker secrets



Website:
- Decide on platform (Apache + MySQL & PHP & JS modules || NodeJS)
- Layout for Website Directories
- Generate LetsEncrypt pem + key on startup



Setting up for Docker Launch for Website Testing:
git clone https://github.com/DevelopingHippo/HoneyWall
cd HoneyWall/Website/
docker build -t web-test .
docker run -p 8080:8080 -d web-test






Website and Application Uses:

Website:
- https://www.youtube.com/watch?v=hRoiG4ZIzeM (Creating a Dynamic Geographic Map in Javascript)